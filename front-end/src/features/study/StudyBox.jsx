import { useState } from "react";
import "./StudyBox.css";

function StudyBox({ selectedDeckId }) {


return (
    <div>
        {selectedDeckId}

        {/* study choice by 'check' for each type */ 

        /* masive by chose types, queue do random sort */
        /* for restudy make new field - timestamp, before new card do check time by the field */
        /* think about realisation 2 mods of study: without restudy and with. if without do not change queue order*/ 
        /* formula to calculate next study time*/ 
        }
    </div>
);

}


export default StudyBox;